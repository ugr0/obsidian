# 構文
```sql
CREATE INDEX index_name
    ON tbl_name (col_name, ...)
```

## ユニークインデックスを張る
```sql
CREATE UNIQUE INDEX index_name
    ON tbl_name (col_name, ...)
```

# 参考記事
- [MySQL | インデックスを作成する(CREATE INDEX文, ALTER TABLE文)](https://www.javadrive.jp/mysql/index/index1.html)